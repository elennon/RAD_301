



@section scripts {
        <script type="text/javascript">
            $(function () {
                function successMessage() {
                    alert("we are 138");

                }
                $("#subButton").click(function () {
                    alert("we are 138");
                });
            });
        </script>
    }

<h2>Add a Guest</h2>

                <div class="col-sm-6" role="form" >
                    @*@using (Ajax.BeginForm("AddGuest", "Home", new { id = @ViewBag.LegId }, new AjaxOptions { HttpMethod = "POST", OnSuccess = "successMessage()" },
                            new { @class = "form-horizontal", @role = "form" }))
                    {
                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary(true)*@
                    @Html.DropDownList("gts", (SelectList)(ViewBag.allGuests), new { @class = "form-control" })
                    <button type="button"  class="btn btn-default" id="subButton">Add Guest</button>        
                    
                </div>

@*@using (Html.BeginForm("AddGuest","Home", FormMethod.Post,
    new { @class = "form-horizontal", @role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    
            <div class="form-horizontal" role="form">
                  <div class="form-group">
                    <label  class="col-sm-4 control-label">Guest name</label>
                    <div class="col-sm-8">
                      <input type="text" class="form-control" name="Name" readonly="true" placeholder="name">
                    </div>
                  </div>
                @Html.DropDownList("gts", (SelectList)(ViewBag.allGuests), new { @class = "form-control" })   

                  <div class="form-group">
                    <div class="col-sm-offset-2 col-sm-8">
                      <button type="submit" class="btn btn-default" id="subButton" >Add Guest</button>
                    </div>
                  </div>                   
            </div>
}*@
